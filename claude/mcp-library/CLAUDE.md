# MCP Library

A parking spot for MCP (Model Context Protocol) server configurations you
want to reuse across projects.

This directory is currently empty. Drop `.mcp.json` files or per-server
config fragments here and reference them from project-specific configs.

Never commit `.mcp.json` files containing credentials — gitignore them and
check in a `.template` sibling instead.
