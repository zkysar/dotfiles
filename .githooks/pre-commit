#!/usr/bin/env bash
# Dotfiles pre-commit: run the repo self-tests.
# Invoked by the global hook at ~/.config/git/hooks/pre-commit via core.hooksPath.
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
exec "$REPO_ROOT/bin/test"
